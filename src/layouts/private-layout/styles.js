import styled from "styled-components";

export const StyledBody = styled.div`
    margin-top: 80px;
    z-index: 1;
    width: 100%;
    height: calc(${window.innerHeight} - 80)px;
    overflow-y: scroll;
`